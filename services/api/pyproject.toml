[project]
name = "dcim-api"
version = "0.1.0"
description = "NOC dashboard gateway and public API façade."
requires-python = ">=3.12"
dependencies = [
    "asyncpg==0.30.0",
  "fastapi==0.116.0",
    "httpx==0.28.1",
    "prometheus-client==0.26.0",
    "pydantic==2.9.2",
    "uvicorn[standard]==0.30.6",
]

[tool.ruff]
extend = "../../ruff.toml"
