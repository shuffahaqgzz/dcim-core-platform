[project]
name = "dcim-cmdb"
version = "0.1.0"
description = "Configuration-item identity, relationships, and context service."
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.0",
    "pydantic==2.9.2",
    "uvicorn[standard]==0.30.6",
]
# Pins are verified at first install in Phase 2; dependencies are declared but not installed in Phase 0.

[tool.ruff]
extend = "../../ruff.toml"
