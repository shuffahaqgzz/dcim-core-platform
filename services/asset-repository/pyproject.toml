[project]
name = "dcim-asset-repository"
version = "0.1.0"
description = "Stable physical/logical asset identity and lifecycle service."
requires-python = ">=3.12"
dependencies = [
    "asyncpg==0.30.0",
    "fastapi==0.141.1",
    "starlette==1.3.1",
    "httpx==0.28.1",
    "prometheus-client==0.26.0",
    "pydantic==2.9.2",
    "uvicorn[standard]==0.30.6",
]

[tool.ruff]
extend = "../../ruff.toml"
