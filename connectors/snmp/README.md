# SNMP Connector

Development starts with synthetic fixtures and SNMPv3-oriented contracts. A future connected adapter requires dedicated read-only identity, allowed OIDs, bounded polling/trap handling, source-impact metrics, kill switch, and tests proving SNMP SET is unavailable.
