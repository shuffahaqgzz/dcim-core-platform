# Redfish Connector

Development starts with synthetic fixtures. A future connected adapter exposes an explicit GET/read allowlist, TLS verification, timeouts, bounded retries, polling ceilings, kill switch, lineage, and negative tests proving write/action/power/reset/firmware methods are unavailable.
