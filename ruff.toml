target-version = "py312"
line-length = 120

[lint]
select = ["E", "F", "W", "I", "UP", "B", "C4"]
# E501 off: no formatter enforced; long evidence/log lines are intentional for grep-ability
ignore = ["E501"]
